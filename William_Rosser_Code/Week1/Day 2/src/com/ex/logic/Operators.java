package com.ex.logic;

public class Operators {
	/*
	 * A java operator is a special symbol that can be applied to a set of
	 * variables, values, or literals - operands - and return a result.
	 * 
	 * Unary, binary, and ternary operators with 1,2,3 operands respectively.
	 */
	
	//Unary
	
		//post-unary operators (output, then change)
		//count++ is count = count + 1
		//count-- is count = count - 1
		
		//pre-unary (change, then output)
		//++count
		//--count
		
		//~n 2's compliment is n= -(n-1);
		//= is assignment
		//- is negation
		//! is boolean not
	
	//Binary
		// * is multiplication
		// / is devision
		// % is modulo
		// shift operators:
			// << is signed left shift. X << N shifts X's bits N spaces left.
			// >> is signed right-shift. X >> N shifts X's bits N spaces right and preserves the 2's compliment sign.
			// >>> is unsigned right-shift.
		// | bitwise or
		// & bitwise and
		// ^ bitwise xor
		// == equals
		// < less than
		// > greater than
		// != not equals
		// && short circuit and
		// || short circuit or
		
	//Ternary operators: [expression]?[if yes]:[if no]
	
	
//	public static void main(String[] args) {
//		int x = 5;
//		System.out.println(x);
//		System.out.println(x++);
//		System.out.println(x);
//		System.out.println(++x);
//		System.out.println(~x);
//		System.out.println(1_000);
//	}
}
