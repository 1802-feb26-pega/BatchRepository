package com.ex.logic;

public class Operators {
	/*
	 * Java Operators are applied to Variables, Values, Literals.
	 * Referred to as Operands
	 * Return a result
	 * 
	 * Unary - Binary - Ternary
	 	* - 1,2,3 operands.
	 		*Unary -  affects one thing*/
	 			void postUnary (int count) {
	 				count++; //count +1
	 				count--; //count -1
	 			}
	 			void preUnary (int count) {
	 				++count;
	 				--count;
	 			}
	 		/*Ternary
	 		 	* (expression) ? (if yes) : (if no)
	 		 	*  
	 		 */
	public static void main(String[] args) {

	}
}
